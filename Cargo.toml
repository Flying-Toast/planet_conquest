[package]
name = "planetary_conquest"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.9", features = ["dynamic"] }
shader_background = { path = "./crates/shader_background" }
